import getMuiTheme from 'material-ui/styles/getMuiTheme';
import { teal100, grey900, blue500 } from 'material-ui/styles/colors';

const muiTheme = getMuiTheme({
  palette: {
    primary1Color: blue500,
    // textColor: grey900
  }
});

export default muiTheme;
